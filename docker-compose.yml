version: "3.9"

services:
  ui:
    build: .
    container_name: appointment-ui
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - NEXT_PUBLIC_API_BASE=http://localhost:4000
    volumes:
      - .:/app
      - /app/node_modules
