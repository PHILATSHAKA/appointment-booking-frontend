version: "3.9"
services:
  ui:
    build: .
    container_name: appointment-ui
    ports:
      - "3000:3000"
    env_file:
      - .env
    environment:
      - NODE_ENV=production
    restart: always
    command: npm run start
